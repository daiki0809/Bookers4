<div class="page_box">

<% if false %>----ここから---------<% end %>
<%= render 'users/sidebar', post: @post %>
<% if false %>-------ここまで------<% end %>

<main>
	<h1>Users</h1>
	<table>
		<tr>
			<th>image</th>
			<th>name</th>
			<th></th>
		</tr>
		<% @user.each do |user| %>
		<tr>
			<td><%= attachment_image_tag user, :profile_image,fallback: "no_image.jpg", class: "main_user_img" %></td>
			<td><%= user.name %></td>
			<td><%= link_to 'show', user_path(user.id) %></td>
		</tr>
		<% end %>
	</table>
<main>
</div>